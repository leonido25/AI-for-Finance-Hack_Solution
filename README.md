# 🚀 Financial RAG Agent: Умный помощник для банковского сектора

![Python](https://img.shields.io/badge/Python-3.7%2B-blue)
![FAISS](https://img.shields.io/badge/Search-FAISS-green)
![LLM](https://img.shields.io/badge/AI-Llama%20%7C%20Gemma%20%7C%20Mistral-purple)
![Hackathon](https://img.shields.io/badge/Event-AI%20for%20Finance%202025-orange)

---

## 💡 О проекте

В рамках хакатона **AI for Finance Hack 2025** мы создали RAG-агента, который решает главную проблему банковских чат-ботов — **галлюцинации и общие фразы**.

Наш агент не просто находит текст, он понимает структуру документа, взвешивает важность каждого слова и **сам оценивает качество** своего ответа перед тем, как показать его пользователю.

### 🔥 Ключевые моменты 

* **🧠 Гибридный поиск 2.0:** Мы объединили векторную мощь **FAISS** с классическим поиском по ключевым словам. Результат: система находит документы, где важен смысл, и не теряет те, где важны точные термины (проценты, сроки).
* **Hierarchical Parsing:** Мы не режем текст на случайные куски. Алгоритм парсит Markdown, сохраняя иерархию заголовков (`##`, `###`), чтобы контекст никогда не терялся.
* **⚖️ Self-Evaluation Loop:** Агент — свой самый строгий критик. Он проверяет сгенерированный ответ по 4 метрикам (структура, факты, длина, контекст) и переписывает его, если оценка низкая.
* **Умный отбор контента:** Система автоматически вычленяет из текста «финансовые маркеры» (ставки, законы, лимиты), отдавая им приоритет при генерации.

---

## ⚙️ Как это работает
Пайплайн обработки запроса выглядит так:

1.  **Загрузка и "Умный" чанкинг**
    * Скрипт загружает `train_data.csv`.
    * Текст разбивается на логические блоки, при этом заголовки "приклеиваются" к контенту, чтобы не потерять смысл раздела.

2.  **Индексация (FAISS)**
    * Создаются эмбеддинги через OpenAI-compatible API.
    * Строится индекс `IndexFlatIP` (Inner Product) для молниеносного косинусного поиска.

3.  **Обработка вопроса (RAG + Rerank)**
    * **Шаг 1:** Поиск топ-кандидатов в векторном пространстве.
    * **Шаг 2:** Реранкинг. Мы пересчитываем релевантность, добавляя вес за совпадение ключевых слов. Формула успеха: `0.7 * similarity + 0.3 * keyword_score`.

4.  **Генерация и Валидация**
    * LLM получает обогащенный контекст и жесткий системный промпт: "Отвечай структурировано, используй заголовки и списки".
    * **Post-processing:** Ответ оценивается алгоритмом. Если в нем нет конкретики (цифр, ссылок на законы) или плохая структура — модель получает "штраф" и сигнал к исправлению.

---

## 📊 Выводы и Результаты

Реализованный подход показал значительное преимущество перед стандартным RAG:

1.  **Борьба с "водой":** Благодаря метрике `specificity_score` (поиск цифр и законов), агент старается давать фактурные ответы, а не общие рекомендации "обратиться в отделение".
2.  **Структура решает:** Принудительное требование Markdown-разметки в промпте и валидаторе делает ответы читаемыми (списки, болд, заголовки).
3.  **Устойчивость:** Наличие `create_fallback_answer` гарантирует, что даже при сбое API или отсутствии контекста пользователь получит корректную заглушку, а не ошибку кода.

---

## 🚀 Возможные улучшения (Roadmap)

Что можно сделать, чтобы превратить прототип в Production-решение:

* **Внедрить Cross-Encoder:** Сейчас реранкинг основан на пересечении слов. Замена на полноценную модель Cross-Encoder (например, `ms-marco-MiniLM`) даст прирост точности в 10-15%.
* **История диалога:** Добавить память цепочки сообщений (Conversation Buffer), чтобы клиент мог задавать уточняющие вопросы.
* **Асинхронность:** Переписать `process_questions_advanced` на `asyncio`, чтобы обрабатывать запросы параллельно, а не последовательно.
* **GraphRAG:** Использовать графы знаний для связывания сложных банковских сущностей (например, связь "Кредитная карта" -> "Льготный период" -> "Условия").

---

## 🛠 Запуск проекта
pip install -r requirements.txt





